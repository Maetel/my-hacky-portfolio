export const terminalHeader = "> " as const;
export const terminalTrailer = "_" as const;
export const terminalBlinkInterval = 500 as const;
export const epsilon = 10e-6 as const;
export const System = {
  fontSize: 16,
  font: "Arial",
  lineHeight: 16,
  textMinPadding: 4,
};
